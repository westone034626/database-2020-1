<div class="w-75 p-3" style="background-color: #eee;">
    <h2>멘토 독서 권수: 
        <span class="badge badge-secondary">
            <%= "#{@books.count}권" %>
        </span>
    </h2>
        <table class="table">
            <th>도서명</th>
            <th>저자</th>
            <th>페이지 수</th>
            <th>장르</th>
            <th>걸린 일수</th>
            <% @books.each do |book| %>
                <tr>
                    <td><%= book.title %></td>
                    <td><%= book.author %></td>
                    <td><%= book.page %></td>
                    <td><%= book.genre %></td>
                    <td><%= book.ct %></td>
                </tr>
            <% end %>
        </table>
</div>